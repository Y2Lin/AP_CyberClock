<p align="right">
  <strong>简体中文</strong> · <a href="TODO.md">English</a>
</p>

# TODO - 下一阶段任务清单

供下次开发会话快速接续的工作记录（创建于 2026-09-04）。当前状态快照：固件 v10.4.2（commit `855138f`）已发布并部署；社区提交（项目 138，revision 218）审核中；替换封面已备好，但审核期间无法上传。发布流程见 [development/publish-to-community.md](development/publish-to-community.md)。

## 1. 重做 DOWN 精简模式（先出设计图，暂不写代码）

- 目标：精简模式只显示日期（`YYYY-MM-DD`）与时间（`HH:MM:SS`）；去掉其余一切元素（外框、上下刻度尺、顶栏状态行、秒数进度柱、频谱条、终端行、像素心形、电池条、底部数据流）。
- 约定流程：先产出 HTML 设计图（沿用 v10.3 设计页的做法），与创作者迭代到定稿，之后才动固件。
- 实现入口：`main/cyber_clock.c` 表盘页的精简分支；五套主题必须全部可用（纸墨主题保持静态，无残影、无故障特效）。
- 待定设计问题：精简模式下数字残影/发光保留还是去掉；日期与时间的纵向构图；秒数原地跳动还是细进度线。

## 2. 电池电量不准确

- 先向创作者收集具体现象（偏高 / 偏低 / 跳变 / 充电时不准？）。
- 排查路径：BSP 的电池读取链路（用的哪个传感器、I2C 还是 ADC、轮询周期）、电压→百分比映射、锂电 OCV 曲线（3.0-4.2V）、负载电压与静置电压之差、滤波与迟滞。
- 已知数据点：表盘把 3.92V 映射为 73%，而常见锂电 OCV 表中 3.92V 约在 60-70%，映射曲线需对实际电芯校准。

## 3. 踩坑笔记放到 AP_Sound_Test 的分支

- 把 v10.4 → v10.4.2 的 `FAP_SCREENSHOT_V1` 全程经验整理成文，发布到 https://github.com/Y2Lin/AP_Sound_Test 的新分支（提交前先看该仓库的文档规范）。
- 候选主题：
  1. `usb_serial_jtag` 驱动默认 TX 环形缓冲只有 256 字节；底层 `xRingbufferSend` 要求条目连续放入，单次 2KB 写入会立刻失败。修复：驱动安装时配 4KB TX 环、按 1KB 分片发送。
  2. VFS 控制台路径会把 `\n` 翻译成 `\r\n`，任何日志字节都会污染二进制像素流：传输窗口内全局静默日志。
  3. LVGL 9.2 没有公开的刷新回调 getter：引入 `display/lv_display_private.h` 直读 `disp->flush_cb`；钩子要有兜底卸载，防止把包装函数误存为“原回调”造成无限递归。
  4. 真机上 150KB 整帧分配必然失败：改用 3 格环形槽流式（共 34.5KB），生产侧背压小睡、双侧共享中止标志。
  5. Web Serial 主机侧坑：DTR/RTS 保持常低（否则关闭串口的电平边沿会触发 ESP32-C3 下载复位）；用单读者泵循环而不是并发竞争读；每次读取都要有超时；设备错误行要上抛显示而不是静默丢弃。
  6. 机器可读的失败原因（`ERR SHOT` 后接 `BUSY` / `MEM` / `HOOK` / `DATA` / `STALL` / `USB` / `SHORT`）让现场失败一眼可诊。
  7. 发布流程事实：截屏回执 24 小时有效；已有版本在审时新版本会被拒（“已有版本正在审核”）。
- 注意：本仓库已有经验条目目录 `docs/experiences/`（见 [development/experience-notes.md](development/experience-notes.md)），需要划分哪些留在本仓库、哪些去 AP_Sound_Test 分支。

## 发布跟进（暂被阻塞）

- Revision 218 审核中。审核出结果（通过或驳回）后，用备好的替换封面（精确 3:4，SHA-256 前缀 `4d405e9d`）重新提交：仅换封面，其余字段与已批准内容完全一致。在此之前重提会被“已有版本正在审核”拒绝。
