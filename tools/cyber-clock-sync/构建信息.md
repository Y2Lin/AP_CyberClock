# 构建信息 · 赛博朋克时钟固件

## 构建环境

| 项目 | 值 |
|---|---|
| 基础项目 | [folotoy/ai-passport](https://github.com/folotoy/ai-passport)（main 分支，commit `4662144`） |
| ESP-IDF | v5.5.3（官方发布版） |
| 目标芯片 | ESP32-C3（riscv32-esp-elf gcc 14.2.0） |
| 构建方式 | 官方 `./tools/validate.sh --firmware`（干净 sdkconfig，非增量构建） |
| 构建日期 | 2026-09-02（v7：重启后如实显示未同步——无 RTC 电池，恢复时间不可信；未同步时时钟文字闪烁提醒） |

## 固件清单与校验

| 文件 | 大小 (字节) | 刷入地址 | SHA256 |
|---|---|---|---|
| `FoloToy-AI-Passport-full.bin` | 1,768,592 | `0x0` | `dad3563fd10489ec640e2d893e0709dfc53d90ab05f699d87176c4cdd18e3bd4` |
| `bootloader.bin` | 22,672 | `0x0` | `d5f8305f250dde3ae3078e37e83d2d6897612d5e76c814267f3c172b0eb4d4a0` |
| `partition-table.bin` | 3,072 | `0x8000` | `71ff0aa394f208066482d1957ae7b094bfd25186a12bf843e05654ffdf110c3a` |
| `FoloToy-AI-Passport.bin` | 1,703,056 | `0x10000` | `a2f7fedc6cc846d54417b975e0d82fdde811e3bd25141a03548c67f2089570ae` |

> 分段固件从合并固件按 `verify_firmware.py` 校验过的字节区间提取，与构建产物逐字节一致。
> 分区表与 v4/v5/v6 完全相同（SHA256 一致），从旧版升级只需刷应用段 `FoloToy-AI-Passport.bin`（`0x10000`）。

## 构建校验结果（全部通过）

```
Verified bootloader/bootloader.bin:      22672 bytes at 0x0
Verified partition_table/partition-table.bin: 3072 bytes at 0x8000
Verified FoloToy-AI-Passport.bin:      1703056 bytes at 0x10000
Mini-program BLE contract: PASS (app 1703056 / 3145728 bytes)
Merged firmware: PASS (1768592 bytes, flash at 0x0)
Firmware build: PASS
```

- 应用 1.69MB，低于 factory 分区 3MB 上限（BLE 安装通道限制）
- 分区表与官方完全一致：`cardid@0x356000`、`recovery@0x700000` 受保护区域未被占用
- 合并固件范围 `0x0 - 0x1BCB00`，结束于 cardid 分区（0x356000）之前，刷入合并固件不会触碰设备身份区
- bootloader 含 5 秒 UP 键 Recovery 手势钩子

## 源码修复说明

### v7（本次）：重启后如实显示未同步（无 RTC 电池的诚实状态）

| 文件 | 改动 |
|---|---|
| `main/time_manager.c` | 初始化时检测重启：`last_sync_ms > 当前单调钟`（重启后单调钟归零，旧记录"在未来"）即判定恢复时间不可信，**清除 `synced` 标志并立即持久化** |
| `main/cyber_clock.c` | 未同步时时钟主文字每秒明暗闪烁（`LV_OPA_60`/`LV_OPA_COVER` 交替），一眼可见"时间未校准" |

设计要点：
- 修复前：断电重启后 `synced=true` 原样从 NVS 恢复，屏幕挂着 `SYNCED`、蓝牙通知与 USB `Q` 都报 `synced:true`——但设备没有 RTC 电池，显示的时间其实是停走的旧值，状态撒谎
- 修复后三处状态源一致（屏幕 / FFC2 通知 / USB `Q`），并联动 v6 省电策略：未同步 → 进时钟页自动开广播等待对时
- 对时一次（USB 或蓝牙）即恢复正常：文字停止闪烁、状态变 `SYNCED`

### v6：BLE 省电策略（未同步自动广播 / 同步后自动关 / 按键开关）

| 文件 | 改动 |
|---|---|
| `main/cyber_clock.c` | ① 进入时钟页仅在**时间不可信**（本次开机未同步过；重启后单调钟归零导致 `is_reliable()` 为假，同样触发）时自动开广播；② 检测 `sync_count` 边沿（BLE / USB 写时间均自增），新同步事件后 5 秒自动 `ble_time_sync_stop()`（断连+停广播+释放 NimBLE）；③ OK 短按由"重启广播"改为**开关切换**；④ 状态栏新增 `BT ADV / BT LINK / BT OFF` 指示 |

设计要点：
- 全部策略逻辑放在 LVGL 定时器 `tick()`（已持锁上下文）执行，`ble_time_sync_stop()` 为同步操作（等 host 任务退出），无跨任务风险；
- 延迟 5 秒再关，保证对端收完最终 FFC2 通知、`Q` 状态回读有窗口；
- 手动开启后不会被中途打断，直到下一次**新的**同步事件才再次自动关；
- 广播期间射频平均电流约 +1~3mA，自动关闭可显著延长电池续航。

### v5：新增 USB 串口对时（免蓝牙方案）

| 文件 | 说明 |
|---|---|
| `main/usb_time_sync.c`（新增） | 利用 ESP32-C3 USB Serial/JTAG 外设接收文本对时命令：`PING`→`PONG` 探活、`T <unix> [tz]`→`OK TS=… TZ=…` 设置时间与时区、`Q`→JSON 状态。任务阻塞在 `usb_serial_jtag_read_bytes()`，无轮询开销 |
| `main/usb_time_sync.h`（新增） | 模块头文件 |
| `main/main.c` | `app_main` 调用 `usb_time_sync_start()`，任意页面（含主菜单）均可 USB 对时，不依赖进入时钟页面 |
| `main/CMakeLists.txt` | 新增源文件与 `esp_driver_usb_serial_jtag` 依赖 |

线程安全设计（吸取 v4 教训）：
- 控制台输出经 `usb_serial_jtag_vfs_use_driver()` 切到驱动路径后，主机未连接时 VFS 写入**快速失败**（`usb_serial_jtag_write()` 检查 `is_connected()` 直接返回），驱动模式下单次阻塞上限 50ms 后丢弃字符——拔线不会卡死任何打日志的任务，不会复现 v4 类死锁；
- USB 对时不直接碰 LVGL：时钟页面每秒自行重读时间，对时后 1 秒内自然刷新。

配套工具：`web-sync.html` 新增"USB 线同步"标签页（Chrome/Edge Web Serial，浏览器直连 USB 对时，Mac 免蓝牙）；`usb-sync.py` 命令行脚本（pyserial，Mac/Windows/Linux 通用）。

### v4：修复"写入时间后整机卡死"

| 文件 | 问题 | 修复 |
|---|---|---|
| `main/cyber_clock.c` | BLE 写入回调 `on_ble_sync()` 运行在 **NimBLE 协议栈任务**中，却直接调用 LVGL 刷新函数（本项目的按键回调都会先拿 `bsp_lvgl_lock()`，此处没有）。与 LVGL 渲染任务并发操作 LVGL 堆 → 死锁/堆损坏。表现为：安卓写入成功后屏幕冻结、按键无响应，BLE 也随之失去响应 | 回调只置 `s_sync_pending` 标志；由 `tick()` 定时器（LVGL 任务上下文，已持锁）检测标志后强制刷新显示。同步结果仍在 100ms 内呈现 |

### v3：修复"手机扫描不到 CyberClock"

| 文件 | 问题 | 修复 |
|---|---|---|
| `main/ble_time_sync.c` | 广播数据超限：flags(3) + 完整名 "CyberClock"(12) + 128 位服务 UUID(18) = **33 字节**，超过 BLE 传统广播包 **31 字节**上限。`ble_gap_adv_set_fields()` 返回 `BLE_HS_EMSGSIZE`，广播从未启动，手机扫描不到设备名 | 设备名留在广播包（15 字节）；128 位 UUID 移入扫描响应包（`ble_gap_adv_rsp_set_fields()`，18 字节）。nRF Connect / LightBlue 主动扫描时仍可按 UUID 筛选 |

### v2：修复 BLE"配对失败"

| 文件 | 问题 | 修复 |
|---|---|---|
| `main/ble_time_sync.c` | GAP 事件处理缺少 `BLE_GAP_EVENT_REPEAT_PAIRING` 分支：手机端旧绑定失配、发起重新配对时协议栈按拒绝处理，手机端显示"配对失败" | 新增该分支：`ble_store_util_delete_peer()` 删除旧绑定后返回 `BLE_GAP_REPEAT_PAIRING_RETRY`（对齐官方 bleprph 示例） |
| `main/ble_time_sync.c` | 绑定存储写满时无溢出策略，后续配对可能失败 | 新增 `ble_hs_cfg.store_status_cb = ble_store_util_status_rr`（round-robin 淘汰最旧记录） |

### v1（首次打包）：修复编译错误

| 文件 | 问题 | 修复 |
|---|---|---|
| `main/ble_time_sync.c` | `ble_hs_cfg.sm_event_cb` 成员在 IDF 5.5.3 的 NimBLE 中不存在 | 删除该赋值；将 SM 事件（PASSKEY_ACTION / ENC_CHANGE）处理逻辑合并进 GAP 事件回调 `gap_event()` |
| `main/cyber_clock.c` | `BSP_BTN_EV_CLICK` 枚举名错误 | 改为 BSP 按键头文件定义的 `BSP_BTN_CLICK` |
| `main/cyber_clock.c` | `vTaskDelay` / `pdMS_TO_TICKS` 隐式声明 | 补充 `freertos/FreeRTOS.h`、`freertos/task.h` 头文件 |
| `main/cyber_clock.c` | 16 字节缓冲区触发 `-Werror=format-truncation`，且 `NOT SYNCED UTC+8`（18 字节）实际会被截断 | 缓冲区扩至 48 字节 |

> 完整 diff 见 `源码修复补丁.patch`。
